// Anchor translation unit for the shaped-graphics core static library. It keeps the target
// well-formed while the library is small; concrete (non-template) code lives in the per-type
// .cc files (buffer.cc, command_list.cc, context.cc) and grows here as needed.

#include <shaped-graphics/all.hh>

namespace sg
{
} // namespace sg
