// Anchor translation unit for the vulkan backend context. Its method and the
// sg::create_vulkan_context factory are defined inline in the header — backends favor readable,
// low-encapsulation code. Real device-init code lands here as the backend is implemented.

#include <shaped-graphics/backends/vulkan/vulkan_context.hh>

namespace sg::backend::vulkan
{
} // namespace sg::backend::vulkan
