// Anchor translation unit for the vulkan backend static library. All types and the
// sg::create_vulkan_context factory are defined inline in the headers — backends favor readable,
// low-encapsulation code. Real device/resource code lands here as the backend is implemented.

#include <shaped-graphics/backends/vulkan/vulkan_buffer.hh>
#include <shaped-graphics/backends/vulkan/vulkan_command_list.hh>
#include <shaped-graphics/backends/vulkan/vulkan_context.hh>

namespace sg::backend::vulkan
{
} // namespace sg::backend::vulkan
