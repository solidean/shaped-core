// Anchor translation unit for the dx12 backend context. Its method and the sg::create_dx12_context
// factory are defined inline in the header — backends favor readable, low-encapsulation code. Real
// device-init code lands here as the backend is implemented.

#include <shaped-graphics/backends/dx12/dx12_context.hh>

namespace sg::backend::dx12
{
} // namespace sg::backend::dx12
