// Anchor translation unit for the dx12 backend static library. All types and the
// sg::create_dx12_context factory are defined inline in the headers — backends favor readable,
// low-encapsulation code. Real device/resource code lands here as the backend is implemented.

#include <shaped-graphics/backends/dx12/dx12_buffer.hh>
#include <shaped-graphics/backends/dx12/dx12_command_list.hh>
#include <shaped-graphics/backends/dx12/dx12_context.hh>

namespace sg::backend::dx12
{
} // namespace sg::backend::dx12
