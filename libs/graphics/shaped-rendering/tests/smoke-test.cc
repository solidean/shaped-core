#include <nexus/test.hh>
#include <shaped-rendering/all.hh>

// shaped-rendering is an early-stage skeleton; this only confirms the library builds, links, and
// its umbrella header compiles. Real tests land alongside the first render routines.
TEST("sr smoke - links")
{
    CHECK(true);
}
