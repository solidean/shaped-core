// The command line the page runs the example with, since a browser gives a wasm module none.
// `--examples` opens nexus's example bucket, and the name selects the one this page was built for.
Module['arguments'] = ['shaped-graphics/rotating-cube', '--examples'];
