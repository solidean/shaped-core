name: macOS Clang

# Tier-2 wide-but-shallow gate: macOS arm64, Homebrew LLVM, RelWithDebInfo.
# Single config; the deep debug/release matrix lives in ci-linux-clang.yml.
# The macos-arm-llvm-* presets point at /opt/homebrew/opt/llvm@22 and link
# against Homebrew libc++, so LLVM (and Ninja) come from brew; CMake ships on the
# runner.
# THE FORMULA IS VERSIONED ON PURPOSE: the unversioned `llvm` is whatever Homebrew
# ships today, so a build pinned to it breaks the day upstream moves -- which it
# did, from 22 to 23, with nothing in the repo having changed.
# --toolset 22 stays as the assertion that the formula is the version we think it
# is. Everything runs through dev.py with an explicit preset.
# See docs/guides/ci.md.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install LLVM + Ninja
        run: brew install llvm@22 ninja

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      # Informational only: doctor also probes clangd's compile DB and llvm-cov,
      # which aren't needed for build+test and would fail a fresh runner.
      - name: Doctor (toolchain info)
        run: uv run dev.py doctor --preset macos-arm-llvm-relwithdebinfo --toolset 22
        continue-on-error: true

      - name: Build
        run: uv run dev.py --collect-logs build/ci-logs.zip build --preset macos-arm-llvm-relwithdebinfo --toolset 22 --diag-archive build/ci-diag.zip --keep-going

      - name: Test
        run: uv run dev.py --collect-logs build/ci-logs.zip test --preset macos-arm-llvm-relwithdebinfo --toolset 22 --merged-xml-report build/ci-test-results.xml

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: macos-clang-diagnostics
          path: |
            build/ci-test-results.xml
            build/ci-diag.zip
            build/ci-logs.zip
          if-no-files-found: warn
